@model IEnumerable<BookCompanyManagement.Models.Paper>

@{
    Layout = null;
}

<table>
    @foreach (var item in Model)
    {
        <tr>
            <td style="border-top:0px">
                @Html.DisplayFor(modelItem => item.PaperSpec.PaperSpecName)

            </td>
        </tr>
    }
</table>
